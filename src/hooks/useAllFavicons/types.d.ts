import { Key } from '../../utils/types';


interface Icons {
  icons: {
    edges: [
      {
        node: {
          fluid: {
            src: Record<Key, never>;
          };
				};
			},
		];
	};
}

export { Icons };
