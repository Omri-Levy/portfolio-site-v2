import {makeStyles} from '../../deps';

const useStyles = makeStyles((theme) => ({
    root: {
        display: 'flex',
        alignItems: 'center'
    },
    icon: {
        color: '#eee'
    }
}));

export default useStyles;
