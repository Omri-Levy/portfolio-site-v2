import styled, { css } from 'styled-components';
import { Box } from '@material-ui/core';

const StyledSubmitContainer = styled(Box)(() => {

	return css`
    justify-self: flex-end;
	`;
});

export default StyledSubmitContainer;
