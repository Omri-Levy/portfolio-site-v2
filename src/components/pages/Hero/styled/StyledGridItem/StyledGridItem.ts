import styled, { css } from 'styled-components';
import { motion } from 'framer-motion';
import { Box } from '@material-ui/core';

const StyledGridItem = styled(motion(Box))(() => {
	return css`

	`;
});

export default StyledGridItem;
