import {makeStyles} from '../../../deps';

const useStyles = makeStyles((theme) => ({
    root: {
        margin: 0,
        padding: 0
    },
    option: {
        color: '#3e96ff'
    }
}));

export default useStyles;
