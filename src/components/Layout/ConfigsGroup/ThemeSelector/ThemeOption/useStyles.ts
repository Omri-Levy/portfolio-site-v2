import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
	iconButton: {
		margin: 0,
		padding: 0,
	},
	menuItem: {
		padding: 0,
		margin: 0,
	},
	option: {
		color: `#3e96ff`,
	},
}));

export default useStyles;
