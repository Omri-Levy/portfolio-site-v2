import {makeStyles} from 'src/deps';

const useStyles = makeStyles(() => ({
    root: {
        margin: 0,
        padding: 0
    },
    option: {
        color: '#3e96ff'
    }
}));

export default useStyles;
