import {makeStyles} from '../../../deps';

const useStyles = makeStyles((theme) => ({
    menuList: {
        display: 'flex',
        margin: 0,
        padding: 0
    }
}));

export default useStyles;
